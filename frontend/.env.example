# Copy this file to `.env` for local overrides, or set this variable in your
# hosting platform's project settings (Vercel/Netlify/etc). Never commit `.env`.

# Base URL of the deployed backend API, including the /api suffix.
# Example: https://ai-career-advisor-api.onrender.com/api
VITE_API_URL=http://localhost:8000/api
